{
  "name": "password-generator",
  "version": "1.0.0",
  "description": "Password generator allows users to create random passwords on the fly from their browser.",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "client": "cd client && npm run serve",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=true npm install --prefix client && npm run build --prefix client"
  },
  "author": "Jose Hernandez",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "helmet": "^4.6.0"
  },
  "engines": {
    "node": "15.5.1"
  }
}
